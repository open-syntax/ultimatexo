meta {
  name: Create a new Room
  type: http
  seq: 5
}

post {
  url: http://{{host}}:{{port}}/rooms
  body: json
  auth: inherit
}

body:json {
  {"is_public": true}
}

script:post-response {
  let roomId = res.getBody().room_id;

  bru.setEnvVar("room_id", roomId);

}

settings {
  encodeUrl: true
  timeout: 0
}
